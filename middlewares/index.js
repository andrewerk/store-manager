const errorHandler = require('./errorHandler');
const addProductValidation = require('./addProductValidation');
const addSaleValidation = require('./addSaleValidation');

module.exports = {
  errorHandler,
  addProductValidation,
  addSaleValidation,
};
